#include <stdio.h>

int main() {

    // Opérateurs arithmétiques = + - * / % ++ -- 
    int x = 10;
    int y = 3;
    int z = 0;

    //z = x + y; // addition
    //z = x - y; // soustraction
    //z = x * y; //multiplication
    //z = x / y; //division
    //z = x % y; // reste de la division

    //x++; incrément de 1
    //x--; décrément
//--------------------------------------------------------------------------------

    //x += 3; / x = x + 3 // ajouter à la variable et la réattribuer
    //x -= 3;
    //x *= 2;
    //x /= 2;
    printf("%d\n", x);
    return 0;
}
