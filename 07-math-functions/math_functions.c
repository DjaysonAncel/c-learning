#include <stdio.h>
#include <math.h> // Fonction mathématique

int main() {

    int x = 45;

    /*  x = sqrt(x); // Racine Carrée
        x = pow(x, 4); // Puissance
        x = round(x); //arrondir
        x = ceil(x); // arrondir à la hausse
        x = floor(x); // arrondir à la baisse 
        x = abs(x); // retourne la valeur absolue d'un entier
        x = log(x); //logarithme naturel

      ---------------Trigonométrie--------------
        x = sin(x);
        x = cos(x);
        x = tan(x);
    */


    printf("%f", x);
    return 0;
}
